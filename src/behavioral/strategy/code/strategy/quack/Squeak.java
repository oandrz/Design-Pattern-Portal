package behavioral.strategy.code.strategy.quack;

// We implement the specific behavior for quack behavior
public class Squeak implements QuackBehavior {
    @Override
    public void quack() {
        // We implement the squeak behavior
    }
}
